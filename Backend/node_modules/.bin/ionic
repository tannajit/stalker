#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<< HEAD
  exec "$basedir/node"  "$basedir/../ionic/bin/ionic" "$@"
else 
  exec node  "$basedir/../ionic/bin/ionic" "$@"
fi
=======
  "$basedir/node"  "$basedir/../ionic/bin/ionic" "$@"
  ret=$?
else 
  node  "$basedir/../ionic/bin/ionic" "$@"
  ret=$?
fi
exit $ret
>>>>>>> 86cd9d4fcb895c5eb154230dbbbb98ea22e9014f
